function selectAllElements($targetForm) {
    title = $targetForm.find(".title")

}


// 글 등록 버튼 클릭 이벤트
function registerBoard($targetForm) {



}

// 등록시 폼 내용 전체 가져오기
function getFormOfRegister() {
    let BoardObj = {
        title : items.$regiTitle,
        writer : items.$regiWriter,
        password : items.$regiPassword,
        content : items.$regiContent
    }

}



$("#regiModal-registerBtn").on('click',function(e){


})

